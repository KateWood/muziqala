<h1> Create New Playlist </h1>

<div class="new-playlist">
<%= form_for @playlist do |p| %>
  <% if @playlist.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@playlist.errors.count, "error") %> prohibited
          this story from being saved:
        </h2>
        <ul>
          <% @playlist.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>

    <% end %>
    <p>
      <%= p.label :name %><br>
      <%= p.text_field :name %>
    </p>
    <p>
      <%= p.submit %> 
    </p>
  <% end %>
<p> Your playlist doesn't have any tracks yet :(</p>
<p> How about you give it a name. Then we'll get the party started</p>
  <iframe src="https://embed.spotify.com/?uri=spotify:trackset:new_playlist:" width="300" height="380" frameborder="0" allowtransparency="true"></iframe>
</div>