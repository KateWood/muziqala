<h1 class="page-header">Artists Matching <%= params[:search_keywords] %></h1>
<ul>
 <% @artists.each do |artist| %>
   <li><%= artist.name %> | <%= link_to "Show", artist_path(artist.id, type: "artist") %></li>
 <% end %>
</ul>

<%= link_to "Back", :back %>