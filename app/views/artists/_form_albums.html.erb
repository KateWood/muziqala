<h1 class="page-header">Albums Matching <%= params[:search_keywords] %></h1>
<ul>
 <% @albums.each do |album| %>
   <li><%= album.name %> | <%= link_to "Show", artist_path(album.id, type: "album") %></li>
 <% end %>
</ul>

<%= link_to "Back", :back %>