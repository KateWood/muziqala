<h1 class="page-header">Albums Matching <%= params[:search_keywords] %></h1>
<ul>
 <% @albums.each do |album| %>
   <li><%= album.artists[0].name %> | <%= link_to album.name, artist_path(album.id, type: "album") %></li>
 <% end %>
</ul>

<%= link_to "Back", :back %>