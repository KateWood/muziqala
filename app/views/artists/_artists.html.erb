<section class="home" data-type="background" data-speed="10">
<h1 class="page-header">Artists Matching <%= params[:search_keywords] %></h1>
<ul>
 <% @artists.each do |artist| %>
   <li ><%= link_to artist.name, artist_path(artist.id, type: "artist") %></li>
   <hr>
 <% end %>
</ul>


<%= link_to "Back", :back %>
</section>